# SERVELECT WORK OS / EMP — environment example

SERVELECT_DATA_PROVIDER=mock
NEXT_PUBLIC_SERVELECT_DATA_PROVIDER=mock

# Set true when the protected app mode should block unauthenticated access.
SERVELECT_REQUIRE_AUTH=false

# Future PostgreSQL / Prisma activation.
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/servelect_work_os?schema=public"

# Optional deployment metadata.
SERVELECT_APP_VERSION=0.9.0
SERVELECT_APP_ENV=local
