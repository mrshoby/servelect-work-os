# SERVELECT WORK OS v1.7.0

Real Task CRUD & API-backed Store.

Apply over repo root, then run:

```powershell
pnpm --filter @servelect/web build
git add .
git commit -m "Add v1.7 real task CRUD and API backed store"
git push origin main
```
