# v8.5.0 Screenshot Audit Report

Generated by:

```powershell
$env:BASE_URL = "https://servelect-work-os-web.vercel.app"
node scripts/audit-v850-screenshots.mjs
```

The v8.5 script writes with `report.join("\n")` and should not repeat the broken newline bug from earlier screenshot scripts.
