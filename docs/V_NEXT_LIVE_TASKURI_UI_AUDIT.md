# V_NEXT Live Taskuri UI Audit

v14 acceptance focuses on GoodDay-like route-specific content. The page shell must remain single-sidebar, but each submenu must now render a distinct view family.

Required proof after Vercel:

- `/taskuri/board` shows a board, not the default table.
- `/taskuri/calendar-gantt` shows timeline/Gantt content.
- `/taskuri/workload-aprobari` shows workload/approval content.
- `/taskuri/reports` shows analytics/report content.
- `/taskuri/forms` shows form/intake content.
- `/taskuri/timesheets` shows time tracking content.
- `/taskuri/files-evidence-v95` shows files/evidence content.
