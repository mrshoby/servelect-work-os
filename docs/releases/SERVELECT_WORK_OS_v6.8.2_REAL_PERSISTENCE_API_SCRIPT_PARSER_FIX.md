# SERVELECT WORK OS v6.8.2 — Real Persistence/API Script Parser Fix

Fixes the PowerShell parser issue caused by escaped quotes inside the apply script audit marker. Keeps the v6.8.0/v6.8.1 persistence API patch files and adds a v6.8.2 smoke test wrapper.
