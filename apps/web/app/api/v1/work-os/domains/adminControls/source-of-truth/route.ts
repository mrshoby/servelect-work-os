import { NextResponse } from "next/server";
import { getSourceOfTruthAdapterActivation } from "@/lib/enterprise/source-of-truth-adapter-activation";
export const dynamic = "force-dynamic";
export async function GET() { const release = getSourceOfTruthAdapterActivation(); return NextResponse.json({ ok: true, domain: release.domains.find((domain) => domain.id === "adminControls") ?? null, adapters: release.adapters.filter((adapter) => adapter.domain === "adminControls"), contracts: release.contracts.filter((contract) => contract.domain === "adminControls"), syncLanes: release.syncLanes.filter((lane) => lane.domain === "adminControls"), reconciliation: release.reconciliation.filter((check) => check.domain === "adminControls"), risks: release.risks.filter((risk) => risk.domain === "adminControls") }); }
