import { createV78RuntimeState, runV78MutationCanary, runV78ProviderProbe, saveV78ServerView, syncV78SavedViews, v78CurrentReadiness, v78GlobalScores, v78ProgressScores, v78RouteList } from "@/lib/enterprise/work-os-v78-provider-telemetry-saved-views";
export async function GET() { const state = runV78ProviderProbe(createV78RuntimeState()); return Response.json({ ok: true, version: state.version, providerTelemetry: state.providerTelemetry, deliveryEvents: state.deliveryEvents }); }
export async function POST() { const state = runV78ProviderProbe(createV78RuntimeState()); return Response.json({ ok: true, version: state.version, mode: "safe_canary", providerTelemetry: state.providerTelemetry }); }
