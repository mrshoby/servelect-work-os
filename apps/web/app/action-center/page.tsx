import { V67GlobalCommandIntegrationClient } from "@/components/work-os/V67GlobalCommandIntegrationClient";

export const metadata = { title: "SERVELECT Work OS · Action Center" };

export default function ActionCenterPage() {
  return <V67GlobalCommandIntegrationClient view="action-center" />;
}
