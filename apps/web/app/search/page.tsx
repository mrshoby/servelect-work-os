import { V67GlobalCommandIntegrationClient } from "@/components/work-os/V67GlobalCommandIntegrationClient";

export const metadata = { title: "SERVELECT Work OS · Global Search" };

export default function SearchPage() {
  return <V67GlobalCommandIntegrationClient view="search" />;
}
